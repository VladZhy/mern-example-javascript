import styled from 'styled-components';

const StyledModalHeaderContainer = styled.div`
  display: flex;
  justify-content: space-between;
  align-items: center;
`;

export default StyledModalHeaderContainer;
