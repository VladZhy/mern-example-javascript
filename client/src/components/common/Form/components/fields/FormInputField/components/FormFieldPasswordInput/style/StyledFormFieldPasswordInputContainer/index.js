import styled from 'styled-components';

const StyledFormFieldPasswordInputContainer = styled.div`
  position: relative;
  display: flex;
  align-items: center;
`;

export default StyledFormFieldPasswordInputContainer;
