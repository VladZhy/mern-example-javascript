import { useFormSubmitButtonPropsProvider } from './hooks';

const FormSubmitButtonPropsProvider = ({ setProps }) => {
  useFormSubmitButtonPropsProvider(setProps);

  return null;
};

export default FormSubmitButtonPropsProvider;
